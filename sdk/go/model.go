package _5minano

// Wallet is a collection of addresses. Each with its own balance and transactions.
type Wallet struct {
}

// Balance is the spendable amount a given wallet or address has.
type Balance struct {
}

// Address is a key pair which can be used to both send and receive Nano.
type Address struct {
}

// Webhook is a message which can be received from _5minano servers notifying about a transaction or something.
type Webhook struct {
}

// Block represents a block in Nano ledger. It's a synonym of transaction.
type Block struct {
}

// Amount represents a Nano amount.
type Amount struct {
}
